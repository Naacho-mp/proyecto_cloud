services:
  fastapi:
    build:
      context: ./servicio2
    container_name: servicio_fastapi
    ports:
      - "8001:8000"
    env_file:
      - ./servicio2/.env        
    restart: unless-stopped

